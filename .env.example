# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=pei
DB_PASSWORD=pei_dev_password
DB_NAME=platform_events

# OpenAI API key for embeddings (optional - mock embeddings used if not set)
OPENAI_API_KEY=

# Authentication token (for testing MCP tools)
# See scripts/seed.ts for test tokens
PEI_AUTH_TOKEN=pei_university_health_token
