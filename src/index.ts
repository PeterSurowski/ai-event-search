// Main entry point for the Platform Event Intelligence service
//
// This service can run in two modes:
// 1. MCP server (default) - bun run mcp:dev
// 2. HTTP API server - bun run dev (not yet implemented)
//
// For now, see src/mcp/server.ts for the MCP server implementation.

console.log('Platform Event Intelligence Service');
console.log('Run `bun run mcp:dev` to start the MCP server');
